<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>News</title>
</head>
<body>
  <h1>헤드라인 뉴스</h1>
  <hr>
  <div style="text-align: right;">
    <button type="button" id="btnReg" onclick="regToggle()">뉴스 등록</button>
  </div>
  <form name="regForm" id="regForm" action="/news/register" method="post" style="padding: 10px 20px; box-sizing: border-box; border: 1px solid gray; display: none; margin: 10px 0;">
    <input type="text" name="url" placeholder="img Url" style="width: 70%;"><br>
    <input type="text" name="title" placeholder="title" style="width: 70%;"><br>
    <textarea type="text" name="content" placeholder="content" style="width: 90%; margin: 10px 0;"></textarea><br>
    <button type="submit">등록</button>
    <button type="reset">초기화</button>
  </form>
  <ul style="list-style: none;">
  <% for(let i=0; i<list.length; i++) { %>
    <li style="border-bottom: 1px solid gray; height: 130px; margin-bottom: 10px; overflow: hidden;">
      <img src="<%= list[i].url %>" style="width: 18%; float: left; margin-right: 20px;">
      <span style="border-right: 1px solid gray; padding-right: 5px;"><a href="/news/<%= list[i].nid %>"><%= list[i].title %></a></span>
      <span style="font-size: .7em;"><%= list[i].rdate %></span>
      <p style="border-top: 1px solid gray; margin-top: 5px; font-size: .9em;"><%= list[i].content %></p>
    </li>
  <% } %>
  </ul>
  
  <script>
    function regToggle() {
      const regForm = document.querySelector('#regForm');
      let display = regForm.style.display;
      if(display === 'none') {
        regForm.style.display = 'block'
      }else{
        regForm.style.display = 'none'
        document.querySelectorAll('#regForm input').forEach((input) => input.value = '' )
        document.querySelector('#regForm textarea').value = ''
      }
      document.querySelector('#regForm input[name=url]').focus();
    }
  </script>
</body>
</html>